import { Roboto, Lusitana } from 'next/font/google';

export const roboto = Roboto({
  weight: '400',
  subsets: ['greek'],
});

export const lusy = Lusitana({
  weight: '400',
  subsets: ['latin'],
});
